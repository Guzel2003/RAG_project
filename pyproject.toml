[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-prep"
version = "0.1.0"
description = "Production-ready RAG data preparation pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]